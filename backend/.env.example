# Base host for the nano-banana-fast proxy. Defaults to the official relay.
# NANO_BANANA_HOST=http://jeniya.top

# Optional API key if you do not want to send it from the client.
# NANO_BANANA_API_KEY=your_api_key_here

# Optional override for the refinement and generation models.
# NANO_BANANA_REFINEMENT_MODEL=gemini-2.5-flash-image-preview
# NANO_BANANA_GENERATION_MODEL=nano-banana-fast

# Optional fallback models that will be attempted if the primary channel is unavailable.
# NANO_BANANA_REFINEMENT_FALLBACK_MODEL=gemini-2.5-flash-image
# NANO_BANANA_GENERATION_FALLBACK_MODEL=gemini-2.5-flash-image

# Port for the Express server (default: 4000)
# PORT=4000
